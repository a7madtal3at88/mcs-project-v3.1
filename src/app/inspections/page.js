export default function InspectionsPage() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui" }}>
      <h1>Inspections / الكشوفات</h1>
      <p>Coming next: list + search + open old report.</p>

      <a href="/inspections/new" style={{ display: "inline-block", marginTop: 16 }}>
        ➕ New Inspection / كشف جديد
      </a>
    </main>
  );
}
